spring:
  datasource:
    url: jdbc:postgresql://172.18.0.2:5433/smtpz_lab
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.18.04:8761/eureka
  instance:
    preferIpAddress: true
